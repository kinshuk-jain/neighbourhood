{
  "name": "service-common",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/ && tsc --build tsconfig-only-this.json && cp package.json dist/ && cd dist/ && yarn install --prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws/dynamodb-data-mapper-annotations": "^0.7.3",
    "pino": "^6.11.3"
  },
  "devDependencies": {
    "@types/pino": "^6.3.9"
  }
}
