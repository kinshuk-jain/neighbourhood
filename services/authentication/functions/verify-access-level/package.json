{
  "name": "verify-access-level",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/ && tsc --build tsconfig-only-this.json && cp package.json dist/",
    "start:invoke": "../../scripts/run.sh invoke verify-access-level -e 'test-events/verifyAccessLevelEvent.json'",
    "start:run": "../../scripts/run.sh run verify-access-level"
  }
}
